#pragma once

#include <string>
#include <vector>

using namespace std;

/**
 * @brief format string from vector
 * 
 * @param format 
 * @param values 
 * @return string 
 */
string	ft_format(const string& format, const vector<string>& values);